// Browser-side entry for the mainview. The actual UI lives in dist/index.html,
// copied into views/mainview/index.html by the build's copy rule. This file
// only exists to satisfy electrobun's bundler; nothing here runs in the
// deployed app.
console.log('[view] mainview loaded');
